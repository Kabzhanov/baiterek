"use client";
import{useState}from"react";import{constructorStages,intakeFields}from"@/lib/mock-data";import{DynamicForm}from"@/components/form-engine/dynamic-form";
export function ConstructorShell(){const[selected,setSelected]=useState(constructorStages[0].id);return <div className="workspace"><aside className="panel"><h2>Структура</h2><ul className="tree">{constructorStages.map(s=><li key={s.id}><button aria-pressed={selected===s.id} onClick={()=>setSelected(s.id)}>{s.title}<br/><small>{s.steps} шага · {s.fields} полей</small></button></li>)}</ul><button className="button secondary">+ Добавить этап</button></aside><main className="canvas" id="main"><div className="mock">Демо-режим: изменения хранятся локально</div><h1 style={{fontSize:"2rem"}}>Предпросмотр услуги</h1><DynamicForm fields={intakeFields}/></main><aside className="properties"><h2>Свойства</h2><div className="field"><label>Название этапа</label><input defaultValue="Основные сведения"/></div><div className="field"><label>Описание</label><textarea defaultValue="Данные о заявителе"/></div><button className="button">Сохранить</button></aside></div>}
