"use client";
import {useForm} from "react-hook-form";import type{FieldDefinition}from "@/lib/types";
export function DynamicForm({fields}:{fields:FieldDefinition[]}){const{register,handleSubmit,formState:{errors,isSubmitting}}=useForm<Record<string,string>>();return <form className="form-card" onSubmit={handleSubmit(()=>undefined)} noValidate><h2>Расскажите о бизнесе</h2><p className="muted">Шаг 1 из 4 · сведения сохраняются как черновик</p>{fields.map(f=><div className="field" key={f.key}><label htmlFor={f.key}>{f.label}{f.required&&" *"}</label>{f.type==="select"?<select id={f.key} {...register(f.key,{required:f.required})}><option value="">Выберите</option>{f.options?.map(o=><option key={o}>{o}</option>)}</select>:f.type==="textarea"?<textarea id={f.key} placeholder={f.placeholder} {...register(f.key,{required:f.required})}/>:<input id={f.key} placeholder={f.placeholder} {...register(f.key,{required:f.required})}/>} {f.hint&&<small>{f.hint}</small>}{errors[f.key]&&<small role="alert">Заполните обязательное поле</small>}</div>)}<button className="button" disabled={isSubmitting}>Сохранить и продолжить</button></form>}
